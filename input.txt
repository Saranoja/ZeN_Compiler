<source_code>

<libraries>
@export
@calculus
@matrix
@collections
@string
</libraries>

<functions>
Eval(int parameter)
{
	return result;
}
add(int x, int y)
{
	return x+y;
}
mul(float x, float y)
{
	return x*y;
}
</functions>

<collections>
square(length)
{
	square::length;
	string colour;
	int area=mul(length,length);
}
cube(x)
{
	cube::x;
	square side(x);
	string colour;
	string size;
	int volume=mul(side->area,x);
}
</collections>

<declarations>
int a;
int b;
bool ok;
int sum;
int result;
int pos;
int index;
float c;
float d;
char first[20];
char second[30];
char name[100];
string something;
string another;
array arr[10];
matrix mtx[3][3];
square number_one(3);
cube number_two(4);
</declarations>

<actions>
a=7;
b=5;
ok=true;
sum=a+b;
write(sum);
c=2.5;
d=12.5;
result=c*d;
first="ZeN";
second="Compiler";
name=concat(first,second);
something="Banana";
another="a";
pos=find(something, another);
arr=[1 2 3 4];
mtx=[1 2 3; 4 5 6; 7 8 9];
write(number_one->area);
write(number_two->volume);
write(Eval(a+c));

if(a<sum)
{
	a=a+1;
	b=b+1;
}
else
{
	sum=sum+1;
};

a=7;
b=2;

while(b!=a && a>0)
{
	b=add(b,1);
	a=a-1;
};

sum=0;

for(index=0;index<=10;index=index+1)
{
	sum=sum+index;
};
write(sum);
</actions>

</source_code>
